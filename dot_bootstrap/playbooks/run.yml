---
- hosts: all
  vars_files:
    - ../vars/main.yml

  tasks:
    - name: Include base tasks
      include_tasks: ../roles/base.yml
      tags: ["base"]
    
    - name: Include audio tasks
      include_tasks: ../roles/audio_fix_dell.yml
      tags: ["dell"]

    - name: Include virtualization tasks
      include_tasks: ../roles/virtualization.yml
      tags: ["virtualization"]